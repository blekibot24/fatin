class animal {
    constructor(name) {
        this.name = name;
    }
    eat() {
        console.log('${this.name} sedang makan.');
    }
}

class Cat exttends Animal {
    construction(name, color) {
        super(name);
        this.color = color;
    }
    meow() {
        console.log('${this,name} si $'{this.color} mengeong.
    }
}